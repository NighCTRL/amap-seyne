
// defines the values to be expected inside each object returned in the json by the api
export interface ProducteurData  {
    prod_id: number
    prod_name: string
    prod_type: string
    prod_loca: string
    prod_pres: string
}

export default ProducteurData;
